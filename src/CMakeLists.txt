SET( BINARY ${CMAKE_PROJECT_NAME} )
SET( EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/bin )
SET( LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/lib )

add_library(${BINARY} STATIC main.cpp)
add_executable(${BINARY}_run main.cpp)
